<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <style>
        h1 {
            font-size: 36px;
            font-weight: bold;
            margin-bottom: 30px;
        }
        body {
            margin-top: 56px;
            padding-top: 20px;
        }
        .navbar {
            background-color: #f8f9fa;
            height: 3rem;
        }

        .navbar-brand {
            font-weight: bold;
            margin-right: 1rem;
            padding-top: 0.25rem;
            padding-bottom: 0.25rem;
        }

        .nav-link {
            color: #333;
        }

        #first {
            margin-top: 4rem;
        }


        .navbar-nav .nav-link {
            color: #707379;
            font-weight: bold;
        }

        .navbar-toggler {
            border-color: #707379;
        }
        #second{
            max-width: 800px;
            margin-top: 50px;
            margin-bottom: 50px;
        }


    </style>


</head>
<body>
{% block navbar %}
<nav class="navbar navbar-expand-lg navbar-light fixed-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_lecon_index') }}">Les Lecons</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_lecon_index') }}">Accueil</a>
                </li>
                {% if is_granted('ROLE_PROF') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}">Nouveau etudiant</a>
                </li>
                {% endif %}
                {% if is_granted('ROLE_ELEVE') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_mesinscriptions') }}">Mes inscriptions</a>
                </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_user_index') }}">Liste Enseignants</a>
                    </li>
                {% endif %}

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
{% endblock %}
<div class="container mt-4"  id="first" >
    <div class="container mt-5" id="second">

    {% block body %}{% endblock %}
    </div>
</div>
</body>
</html>

