{% extends 'base.html.twig' %}

{% block title %}Lecon{% endblock %}

{% block body %}
    <h1>Lecon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lecon.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lecon.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ lecon.description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_lecon_index') }}">back to list</a>

    <a class="btn btn-warning" href="{{ path('app_lecon_edit', {'id': lecon.id}) }}">edit</a>

    {{ include('lecon/_delete_form.html.twig') }}
{% endblock %}
